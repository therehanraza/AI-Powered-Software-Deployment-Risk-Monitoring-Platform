{
  "name": "ai-powered-software-deployment-risk-monitoring-platform",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Powered Software Deployment Risk Monitoring Platform with release analysis and rollout simulation.",
  "scripts": {
    "setup": "python -m pip install -r backend/requirements.txt && npm --prefix frontend install",
    "dev": "concurrently \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 5000 --app-dir backend",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start:backend": "python -m uvicorn app.main:app --host 0.0.0.0 --port 5000 --app-dir backend",
    "seed": "python backend/scripts/seed.py"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
